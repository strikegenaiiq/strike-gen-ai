# Showcase video behavior

Featured showcase videos are intentionally presented as product proof: they autoplay muted, loop, and play inline when the browser permits autoplay. Controls remain available so visitors can pause or adjust playback.

The public feed remains read-only for visitors. Publishing, changing, or removing showcase content remains an admin-only operation enforced by Supabase RLS.
